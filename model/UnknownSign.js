class UnknownSign {
    constructor(coordinates, user_id, photo, address, direction) {
        this.coordinates = coordinates;
        this.user_id = user_id;
        this.photo = photo;
        this.address = address;
        this.direction = direction;
    }
}

module.exports = UnknownSign;