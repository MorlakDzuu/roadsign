class UnknownSign {
    constructor(coordinates, user_id, photo, address) {
        this.coordinates = coordinates;
        this.user_id = user_id;
        this.photo = photo;
        this.address = address;
    }
}

module.exports = UnknownSign;